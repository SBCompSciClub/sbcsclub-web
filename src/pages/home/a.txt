{loggedin ? (
              <Button variant="contained" color="inherit" onClick={handleLogin}>
                Sign in with Google
              </Button>
            ) : (
                  <IconButton
                    edge="end"
                    aria-label="account of current user"
                    aria-haspopup="true"
                    color="inherit"
                  >
                    <AccountCircle />
                  </IconButton>
            )}